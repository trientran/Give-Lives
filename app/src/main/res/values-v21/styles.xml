<resources>



    <style name="WarnButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:colorButtonNormal">@color/material_red_a200</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme" parent="AppTheme.Base">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--API 19 +, this make status bar darker than appbar/toolbar-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->
<!--Lessions learnt-->
        <!--remove <item name="android:statusBarColor">@android:color/transparent</item> or set android:fitsSystemWindows="true" attribute within DrawerLayout or CoordinatorLayout to fix white (transparent) status bar set in style v21).
        Layouts other than DrawerLayout or Coordinator need to have a theme background for the whole main layout to draw behind status bar or put this code to activity class:
        getWindow().getDecorView().setSystemUiVisibility(
            View.SYSTEM_UI_FLAG_LAYOUT_STABLE
            | View.SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN);-->
    </style>

</resources>
